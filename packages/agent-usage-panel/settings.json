{
  "$schema": "https://raw.githubusercontent.com/Axenide/Ambxst/dev/docs/mods/settings.schema.json",
  "version": 1,
  "fields": [
    {
      "key": "refreshMinutes",
      "label": "Usage refresh interval",
      "description": "Minutes between provider usage reads. Each read runs agent_usage.py against the local provider databases.",
      "type": "integer",
      "default": 15,
      "minimum": 1,
      "maximum": 240,
      "restartRequired": false
    }
  ]
}
